import { LeftSectionWrapper } from './SectionTemplateLeft.styled';

const SectionTemplateLeft = ({ children }) => {
  return <LeftSectionWrapper>{children}</LeftSectionWrapper>;
};

export default SectionTemplateLeft;
