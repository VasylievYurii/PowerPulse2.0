import SectionTemplate from '../components/SectionTemplate/SectionTemplate';

const Products = () => {
  return (
    <SectionTemplate>
      <p>Products page</p>
    </SectionTemplate>
  );
};

export default Products;
