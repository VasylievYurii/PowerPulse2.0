import SectionTemplate from '../components/SectionTemplate/SectionTemplate';

const Profile = () => {
  return (
    <SectionTemplate>
      <p>Profile page</p>
    </SectionTemplate>
  );
};

export default Profile;
